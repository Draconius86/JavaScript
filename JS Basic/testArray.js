function demo() {
    let fifteen = 15;
    let eight = 8;
    let three = 30;
    let numbers = [ fifteen, eight, three ];
    console.log(numbers);
    console.log(numbers.length);
    console.log(numbers[numbers.length - 1]);
}

demo();